# Residual Risk Schema

Residual risk uses the Paper 12-inspired lightweight formula:

`residual_risk = cloud_severity * (1 - mitigation_coverage * control_effectiveness)`

All outputs are synthetic and are intended for prioritization only.
